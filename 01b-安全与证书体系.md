# Kubernetes 安全与证书体系 (PKI)

Kubernetes 是一个天生具有 **“零信任” (Zero-Trust)** 基因的分布式系统。它的所有核心组件之间，默认全部通过 **mTLS（双向 TLS 认证）** 进行通信。这意味着：
- 客户端发起请求时，必须验证服务端是不是伪造的（单向 TLS）。
- 服务端收到请求时，也必须验证客户端到底是谁，是否有权限（双向 TLS 认证身份）。

这就导致了 Kubernetes 集群中存在数量极其庞大、种类繁多的 X.509 证书。为了理清它们，避免在部署或排查 `x509: certificate signed by unknown authority` 时抓狂，我们必须将它们按**三大独立的 CA（证书机构）体系**和一个**非证书密钥对**来分类整理。

在标准的 Kubeadm 部署或主流公有云集群中，相关的证书文件通常存放在 `/etc/kubernetes/pki` 目录下。

---

## 1. 第一套：Etcd 证书体系 (Etcd PKI)

Etcd 存储了集群的所有状态数据和 Secret 凭据，属于集群的“大脑”和最高机密库。为了隔离风险，它通常拥有自己**完全独立的 CA 体系**，不和 K8s 主 CA 混用。

| 证书 / 密钥文件 | 角色 | 用途解析 |
|---|---|---|
| `etcd/ca.crt` | **Root CA** | Etcd 体系的根 CA，用于签发所有 Etcd 相关的证书。 |
| `etcd/server.crt` | **服务端 (Serving)** | Etcd 节点用来对外提供服务（监听 2379 等端口）的证书。 |
| `etcd/peer.crt` | **服务端 + 客户端** | Etcd 集群内部节点之间互相通信、同步 raft 数据时使用的对等证书。 |
| `apiserver-etcd-client.crt` | **客户端 (Client)** | **关键！** 这是 APIServer 的客户端证书。APIServer 拿着它向 Etcd 证明“我是合法的 APIServer”，从而获取读写数据的权限。 |
| `etcd/healthcheck-client.crt` | **客户端 (Client)** | 专用于 Liveness/Readiness 探针检查 Etcd 存活状态的客户端证书。 |

---

## 2. 第二套：Kubernetes 基础证书体系 (K8s General PKI)

这是数量最多、覆盖面最广的一套体系。基于 K8s 主 CA 签发，覆盖了绝大多数控制平面和节点组件的通信。

### 2.1 根 CA 与核心服务端
| 证书 / 密钥文件 | 角色 | 用途解析 |
|---|---|---|
| `ca.crt` | **Root CA** | K8s 体系的主根 CA，签发几乎所有 K8s 组件的证书。 |
| `apiserver.crt` | **服务端 (Serving)** | APIServer 监听 6443 对外提供服务的证书。里面包含多个 SANs (备用名称)：如本机 IP、10.96.0.1 (内部 svc IP)、kubernetes.default.svc、甚至外网 LB 的 VIP。如果请求用的 IP/域名不在里面，就会报 SANs 错误。 |

### 2.2 内部反向通信客户端
| 证书 / 密钥文件 | 角色 | 用途解析 |
|---|---|---|
| `apiserver-kubelet-client.crt` | **客户端 (Client)** | 当你在敲 `kubectl exec / logs` 时，APIServer 需要**主动连接** Node 上的 kubelet。APIServer 就会使用这个证书向 kubelet 证明身份。 |

### 2.3 核心管控组件（只有客户端角色）
像 Controller-Manager 和 Scheduler 这种组件，它们自己**不开放**任何供别人调用的控制端口，它们纯粹只是不断地去调用 APIServer。因此，它们只需要客户端证书。
其权限绑定在证书的 `O` (Organization) 和 `CN` (Common Name) 字段中（例如 `CN=system:kube-scheduler`）。

| 证书配置存放点 | 角色 | 用途解析 |
|---|---|---|
| `controller-manager.conf` | **客户端 (Client)** | 供 kube-controller-manager 进程连接 APIServer 使用。 |
| `scheduler.conf` | **客户端 (Client)** | 供 kube-scheduler 进程连接 APIServer 使用。 |
| `admin.conf` | **客户端 (Client)** | K8s 的最高权限 `system:masters` 证书！这就是你常用的 `~/.kube/config`。谁拿到了它，谁就是集群的“上帝”。 |

### 2.4 Kubelet 证书（位于工作节点）
工作节点（Worker Node）上通常在 `/var/lib/kubelet/pki/` 目录下。
* **kubelet-client-current.pem**：kubelet 连接 APIServer 用来汇报 Node 状态的客户端证书。
* **kubelet-server-current.pem**：kubelet 监听 10250 端口，接受 APIServer 来拉取日志/执行命令时的服务端证书。
> 生产环境中，kubelet 的证书通常由 APIServer 的 `certificates.k8s.io` API 自动签发并定期轮转 (CSR Rotation)。

---

## 3. 第三套：前端代理证书体系 (Front-Proxy PKI)

这一套容易被忽视，它专门用于 **API 聚合层 (Aggregation Layer)** 机制。
当你安装了 `metrics-server`，并执行 `kubectl top node` 时，请求是先打到 APIServer 的，但 APIServer 不懂 `metrics.k8s.io`，它要把请求**代理转发**给 `metrics-server` 这个后端的扩展 APIServer。

| 证书 / 密钥文件 | 角色 | 用途解析 |
|---|---|---|
| `front-proxy-ca.crt` | **Root CA** | 前端代理独立 CA。 |
| `front-proxy-client.crt` | **客户端 (Client)** | APIServer 用这个证书去连接 `metrics-server`，证明“这个请求是我 K8s 主 APIServer 安全过滤后转发过来的，你可以信任我”。 |

---

## 4. 第四套：Service Account 密钥对 (非 HTTPS 证书)

在 PKI 目录下，你会看到 `sa.key` 和 `sa.pub`。**请注意，它们不是用来做 HTTPS 通信的 X.509 证书！** 这只是一对单纯的 RSA 密钥对。

**它们的作用是签发和验证 JWT (JSON Web Token)：**
1. **Controller-Manager 签发**：当系统自动为 Pod 创建身份令牌（挂载到 `/var/run/secrets/kubernetes.io/serviceaccount/token`）时，Controller-Manager 会使用 **`sa.key` (私钥)** 对这个 Token 进行数字签名。
2. **APIServer 验签**：当 Pod 拿着这个 Token 跑来调用 APIServer 接口时，APIServer 使用 **`sa.pub` (公钥)** 来验证这个 Token 是不是伪造的，以及是否过期。

---

## 5. 常见证书报错排查指南

| 错误信息 | 原因与排查方向 |
|---|---|
| `x509: certificate signed by unknown authority` | 客户端缺乏对应的 `ca.crt`。如果你用 curl 访问 APIServer，你需要带上 `--cacert /etc/kubernetes/pki/ca.crt`。 |
| `x509: certificate is valid for 10.x.x.x, not 192.x.x.x` | APIServer 的服务端证书 SANs 列表里没有这个 IP。往往发生在更换了外网 IP 或 LB IP，但没有重新签发 `apiserver.crt` 时。 |
| `error: You must be logged in to the server (Unauthorized)` | 客户端证书过期，或者配置的 `~/.kube/config` 中的 client-certificate-data 不正确。 |
| Node 的 kubelet 报 `Serving certificate is expired` | kubelet 的 Server 证书未开启自动轮转，或者签发 CSR (CertificateSigningRequest) 的审批流程卡住了。 |
